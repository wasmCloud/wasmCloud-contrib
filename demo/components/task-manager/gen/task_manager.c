// Generated by `wit-bindgen` 0.28.0. DO NOT EDIT!
#include "task_manager.h"
#include <stdlib.h>
#include <string.h>

// Imported Functions from `wasi:logging/logging`

__attribute__((__import_module__("wasi:logging/logging"), __import_name__("log")))
extern void __wasm_import_wasi_logging_logging_log(int32_t, uint8_t *, size_t, uint8_t *, size_t);

// Imported Functions from `wasmcloud:postgres/query@0.1.0-draft`

__attribute__((__import_module__("wasmcloud:postgres/query@0.1.0-draft"), __import_name__("query")))
extern void __wasm_import_wasmcloud_postgres_query_query(uint8_t *, size_t, uint8_t *, size_t, uint8_t *);

// Imported Functions from `wasmcloud:postgres/prepared@0.1.0-draft`

__attribute__((__import_module__("wasmcloud:postgres/prepared@0.1.0-draft"), __import_name__("prepare")))
extern void __wasm_import_wasmcloud_postgres_prepared_prepare(uint8_t *, size_t, uint8_t *);

__attribute__((__import_module__("wasmcloud:postgres/prepared@0.1.0-draft"), __import_name__("exec")))
extern void __wasm_import_wasmcloud_postgres_prepared_exec(uint8_t *, size_t, uint8_t *, size_t, uint8_t *);

// Exported Functions from `wasmcloud:task-manager/tracker`

__attribute__((__weak__, __export_name__("cabi_post_wasmcloud:task-manager/tracker#start")))
void __wasm_export_exports_wasmcloud_task_manager_tracker_start_post_return(uint8_t * arg0) {
  switch ((int32_t) (int32_t) *((uint8_t*) (arg0 + 0))) {
    case 0: {
      if ((*((size_t*) (arg0 + 8))) > 0) {
        free(*((uint8_t **) (arg0 + 4)));
      }
      break;
    }
    case 1: {
      if ((*((size_t*) (arg0 + 8))) > 0) {
        free(*((uint8_t **) (arg0 + 4)));
      }
      break;
    }
  }
}

__attribute__((__weak__, __export_name__("cabi_post_wasmcloud:task-manager/tracker#get")))
void __wasm_export_exports_wasmcloud_task_manager_tracker_get_post_return(uint8_t * arg0) {
  switch ((int32_t) (int32_t) *((uint8_t*) (arg0 + 0))) {
    case 0: {
      if ((*((size_t*) (arg0 + 8))) > 0) {
        free(*((uint8_t **) (arg0 + 4)));
      }
      if ((*((size_t*) (arg0 + 16))) > 0) {
        free(*((uint8_t **) (arg0 + 12)));
      }
      if ((*((size_t*) (arg0 + 24))) > 0) {
        free(*((uint8_t **) (arg0 + 20)));
      }
      if ((*((size_t*) (arg0 + 32))) > 0) {
        free(*((uint8_t **) (arg0 + 28)));
      }
      switch ((int32_t) (int32_t) *((uint8_t*) (arg0 + 36))) {
        case 0: {
          break;
        }
        case 1: {
          if ((*((size_t*) (arg0 + 44))) > 0) {
            free(*((uint8_t **) (arg0 + 40)));
          }
          break;
        }
      }
      switch ((int32_t) (int32_t) *((uint8_t*) (arg0 + 48))) {
        case 0: {
          break;
        }
        case 1: {
          if ((*((size_t*) (arg0 + 56))) > 0) {
            free(*((uint8_t **) (arg0 + 52)));
          }
          break;
        }
      }
      switch ((int32_t) (int32_t) *((uint8_t*) (arg0 + 60))) {
        case 0: {
          break;
        }
        case 1: {
          if ((*((size_t*) (arg0 + 68))) > 0) {
            free(*((uint8_t **) (arg0 + 64)));
          }
          break;
        }
      }
      break;
    }
    case 1: {
      if ((*((size_t*) (arg0 + 8))) > 0) {
        free(*((uint8_t **) (arg0 + 4)));
      }
      break;
    }
  }
}

__attribute__((__weak__, __export_name__("cabi_post_wasmcloud:task-manager/tracker#complete")))
void __wasm_export_exports_wasmcloud_task_manager_tracker_complete_post_return(uint8_t * arg0) {
  switch ((int32_t) (int32_t) *((uint8_t*) (arg0 + 0))) {
    case 0: {
      break;
    }
    case 1: {
      if ((*((size_t*) (arg0 + 8))) > 0) {
        free(*((uint8_t **) (arg0 + 4)));
      }
      break;
    }
  }
}

__attribute__((__weak__, __export_name__("cabi_post_wasmcloud:task-manager/tracker#fail")))
void __wasm_export_exports_wasmcloud_task_manager_tracker_fail_post_return(uint8_t * arg0) {
  switch ((int32_t) (int32_t) *((uint8_t*) (arg0 + 0))) {
    case 0: {
      break;
    }
    case 1: {
      if ((*((size_t*) (arg0 + 8))) > 0) {
        free(*((uint8_t **) (arg0 + 4)));
      }
      break;
    }
  }
}

__attribute__((__weak__, __export_name__("cabi_post_wasmcloud:task-manager/tracker#list")))
void __wasm_export_exports_wasmcloud_task_manager_tracker_list_post_return(uint8_t * arg0) {
  switch ((int32_t) (int32_t) *((uint8_t*) (arg0 + 0))) {
    case 0: {
      size_t len = *((size_t*) (arg0 + 8));
      if (len > 0) {
        uint8_t *ptr = *((uint8_t **) (arg0 + 4));
        for (size_t i = 0; i < len; i++) {
          uint8_t *base = ptr + i * 68;
          (void) base;
          if ((*((size_t*) (base + 4))) > 0) {
            free(*((uint8_t **) (base + 0)));
          }
          if ((*((size_t*) (base + 12))) > 0) {
            free(*((uint8_t **) (base + 8)));
          }
          if ((*((size_t*) (base + 20))) > 0) {
            free(*((uint8_t **) (base + 16)));
          }
          if ((*((size_t*) (base + 28))) > 0) {
            free(*((uint8_t **) (base + 24)));
          }
          switch ((int32_t) (int32_t) *((uint8_t*) (base + 32))) {
            case 0: {
              break;
            }
            case 1: {
              if ((*((size_t*) (base + 40))) > 0) {
                free(*((uint8_t **) (base + 36)));
              }
              break;
            }
          }
          switch ((int32_t) (int32_t) *((uint8_t*) (base + 44))) {
            case 0: {
              break;
            }
            case 1: {
              if ((*((size_t*) (base + 52))) > 0) {
                free(*((uint8_t **) (base + 48)));
              }
              break;
            }
          }
          switch ((int32_t) (int32_t) *((uint8_t*) (base + 56))) {
            case 0: {
              break;
            }
            case 1: {
              if ((*((size_t*) (base + 64))) > 0) {
                free(*((uint8_t **) (base + 60)));
              }
              break;
            }
          }
        }
        free(ptr);
      }
      break;
    }
    case 1: {
      if ((*((size_t*) (arg0 + 8))) > 0) {
        free(*((uint8_t **) (arg0 + 4)));
      }
      break;
    }
  }
}

__attribute__((__weak__, __export_name__("cabi_post_wasmcloud:task-manager/tracker#delete")))
void __wasm_export_exports_wasmcloud_task_manager_tracker_delete_post_return(uint8_t * arg0) {
  switch ((int32_t) (int32_t) *((uint8_t*) (arg0 + 0))) {
    case 0: {
      break;
    }
    case 1: {
      if ((*((size_t*) (arg0 + 8))) > 0) {
        free(*((uint8_t **) (arg0 + 4)));
      }
      break;
    }
  }
}

// Canonical ABI intrinsics

__attribute__((__weak__, __export_name__("cabi_realloc")))
void *cabi_realloc(void *ptr, size_t old_size, size_t align, size_t new_size) {
  (void) old_size;
  if (new_size == 0) return (void*) align;
  void *ret = realloc(ptr, new_size);
  if (!ret) abort();
  return ret;
}

// Helper Functions

void wasmcloud_postgres_types_query_error_free(wasmcloud_postgres_types_query_error_t *ptr) {
  switch ((int32_t) ptr->tag) {
    case 0: {
      task_manager_string_free(&ptr->val.invalid_params);
      break;
    }
    case 1: {
      task_manager_string_free(&ptr->val.invalid_query);
      break;
    }
    case 2: {
      task_manager_string_free(&ptr->val.unexpected);
      break;
    }
  }
}

void wasmcloud_postgres_types_statement_prepare_error_free(wasmcloud_postgres_types_statement_prepare_error_t *ptr) {
  switch ((int32_t) ptr->tag) {
    case 0: {
      task_manager_string_free(&ptr->val.unexpected);
      break;
    }
  }
}

void wasmcloud_postgres_types_prepared_statement_exec_error_free(wasmcloud_postgres_types_prepared_statement_exec_error_t *ptr) {
  switch ((int32_t) ptr->tag) {
    case 1: {
      wasmcloud_postgres_types_query_error_free(&ptr->val.query_error);
      break;
    }
    case 2: {
      task_manager_string_free(&ptr->val.unexpected);
      break;
    }
  }
}

void wasmcloud_postgres_types_ipv4_addr_free(wasmcloud_postgres_types_ipv4_addr_t *ptr) {
  task_manager_string_free(ptr);
}

void wasmcloud_postgres_types_ipv6_addr_free(wasmcloud_postgres_types_ipv6_addr_t *ptr) {
  task_manager_string_free(ptr);
}

void wasmcloud_postgres_types_subnet_free(wasmcloud_postgres_types_subnet_t *ptr) {
  task_manager_string_free(ptr);
}

void wasmcloud_postgres_types_xip_list_free(wasmcloud_postgres_types_xip_list_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    int64_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_types_column_name_free(wasmcloud_postgres_types_column_name_t *ptr) {
  task_manager_string_free(ptr);
}

void wasmcloud_postgres_types_numeric_free(wasmcloud_postgres_types_numeric_t *ptr) {
  task_manager_string_free(ptr);
}

void task_manager_option_u16_free(task_manager_option_u16_t *ptr) {
  if (ptr->is_some) {
  }
}

void wasmcloud_postgres_types_option_lexeme_weight_free(wasmcloud_postgres_types_option_lexeme_weight_t *ptr) {
  if (ptr->is_some) {
  }
}

void wasmcloud_postgres_types_lexeme_free(wasmcloud_postgres_types_lexeme_t *ptr) {
  task_manager_option_u16_free(&ptr->position);
  wasmcloud_postgres_types_option_lexeme_weight_free(&ptr->weight);
  task_manager_string_free(&ptr->data);
}

void wasmcloud_postgres_types_offset_free(wasmcloud_postgres_types_offset_t *ptr) {
  switch ((int32_t) ptr->tag) {
    case 0: {
      break;
    }
    case 1: {
      break;
    }
  }
}

void wasmcloud_postgres_types_date_free(wasmcloud_postgres_types_date_t *ptr) {
  switch ((int32_t) ptr->tag) {
    case 2: {
      break;
    }
  }
}

void wasmcloud_postgres_types_interval_free(wasmcloud_postgres_types_interval_t *ptr) {
  wasmcloud_postgres_types_date_free(&ptr->start);
  wasmcloud_postgres_types_date_free(&ptr->end);
}

void wasmcloud_postgres_types_time_tz_free(wasmcloud_postgres_types_time_tz_t *ptr) {
  task_manager_string_free(&ptr->timesonze);
}

void wasmcloud_postgres_types_timestamp_free(wasmcloud_postgres_types_timestamp_t *ptr) {
  wasmcloud_postgres_types_date_free(&ptr->date);
}

void wasmcloud_postgres_types_timestamp_tz_free(wasmcloud_postgres_types_timestamp_tz_t *ptr) {
  wasmcloud_postgres_types_timestamp_free(&ptr->timestamp);
  wasmcloud_postgres_types_offset_free(&ptr->offset);
}

void task_manager_list_s64_free(task_manager_list_s64_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    int64_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void task_manager_list_bool_free(task_manager_list_bool_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    bool *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void task_manager_list_hashable_f64_free(task_manager_list_hashable_f64_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_hashable_f64_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void task_manager_list_hashable_f32_free(task_manager_list_hashable_f32_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_hashable_f32_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void task_manager_list_s32_free(task_manager_list_s32_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    int32_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void task_manager_list_numeric_free(task_manager_list_numeric_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_numeric_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      wasmcloud_postgres_types_numeric_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void task_manager_list_s16_free(task_manager_list_s16_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    int16_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void task_manager_list_list_s16_free(task_manager_list_list_s16_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    task_manager_list_s16_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      task_manager_list_s16_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void task_manager_list_u8_free(task_manager_list_u8_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    uint8_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void task_manager_tuple2_u32_list_u8_free(task_manager_tuple2_u32_list_u8_t *ptr) {
  task_manager_list_u8_free(&ptr->f1);
}

void task_manager_list_tuple2_u32_list_u8_free(task_manager_list_tuple2_u32_list_u8_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    task_manager_tuple2_u32_list_u8_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      task_manager_tuple2_u32_list_u8_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void task_manager_option_u32_free(task_manager_option_u32_t *ptr) {
  if (ptr->is_some) {
  }
}

void task_manager_tuple2_option_u32_list_u8_free(task_manager_tuple2_option_u32_list_u8_t *ptr) {
  task_manager_option_u32_free(&ptr->f0);
  task_manager_list_u8_free(&ptr->f1);
}

void task_manager_list_tuple2_option_u32_list_u8_free(task_manager_list_tuple2_option_u32_list_u8_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    task_manager_tuple2_option_u32_list_u8_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      task_manager_tuple2_option_u32_list_u8_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void task_manager_list_list_u8_free(task_manager_list_list_u8_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    task_manager_list_u8_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      task_manager_list_u8_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void task_manager_list_string_free(task_manager_list_string_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    task_manager_string_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      task_manager_string_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_types_list_mac_address_eui48_free(wasmcloud_postgres_types_list_mac_address_eui48_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_mac_address_eui48_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_types_list_mac_address_eui64_free(wasmcloud_postgres_types_list_mac_address_eui64_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_mac_address_eui64_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void task_manager_list_tuple2_lower_left_point_upper_right_point_free(task_manager_list_tuple2_lower_left_point_upper_right_point_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    task_manager_tuple2_lower_left_point_upper_right_point_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void task_manager_list_tuple2_center_point_radius_free(task_manager_list_tuple2_center_point_radius_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    task_manager_tuple2_center_point_radius_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void task_manager_list_tuple2_start_point_end_point_free(task_manager_list_tuple2_start_point_end_point_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    task_manager_tuple2_start_point_end_point_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void task_manager_list_point_free(task_manager_list_point_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_point_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void task_manager_list_list_point_free(task_manager_list_list_point_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    task_manager_list_point_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      task_manager_list_point_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_types_list_date_free(wasmcloud_postgres_types_list_date_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_date_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      wasmcloud_postgres_types_date_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_types_list_interval_free(wasmcloud_postgres_types_list_interval_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_interval_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      wasmcloud_postgres_types_interval_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_types_list_time_free(wasmcloud_postgres_types_list_time_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_time_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_types_list_time_tz_free(wasmcloud_postgres_types_list_time_tz_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_time_tz_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      wasmcloud_postgres_types_time_tz_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_types_list_timestamp_free(wasmcloud_postgres_types_list_timestamp_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_timestamp_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      wasmcloud_postgres_types_timestamp_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_types_list_timestamp_tz_free(wasmcloud_postgres_types_list_timestamp_tz_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_timestamp_tz_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      wasmcloud_postgres_types_timestamp_tz_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void task_manager_list_u64_free(task_manager_list_u64_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    uint64_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void task_manager_tuple3_xmin_xmax_xip_list_free(task_manager_tuple3_xmin_xmax_xip_list_t *ptr) {
  wasmcloud_postgres_types_xip_list_free(&ptr->f2);
}

void wasmcloud_postgres_types_list_lexeme_free(wasmcloud_postgres_types_list_lexeme_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_lexeme_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      wasmcloud_postgres_types_lexeme_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void task_manager_option_string_free(task_manager_option_string_t *ptr) {
  if (ptr->is_some) {
    task_manager_string_free(&ptr->val);
  }
}

void task_manager_tuple2_string_option_string_free(task_manager_tuple2_string_option_string_t *ptr) {
  task_manager_string_free(&ptr->f0);
  task_manager_option_string_free(&ptr->f1);
}

void task_manager_list_tuple2_string_option_string_free(task_manager_list_tuple2_string_option_string_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    task_manager_tuple2_string_option_string_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      task_manager_tuple2_string_option_string_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_types_pg_value_free(wasmcloud_postgres_types_pg_value_t *ptr) {
  switch ((int32_t) ptr->tag) {
    case 1: {
      break;
    }
    case 2: {
      break;
    }
    case 3: {
      task_manager_list_s64_free(&ptr->val.int8_array);
      break;
    }
    case 4: {
      break;
    }
    case 5: {
      break;
    }
    case 6: {
      break;
    }
    case 7: {
      break;
    }
    case 8: {
      task_manager_list_bool_free(&ptr->val.bool_array);
      break;
    }
    case 9: {
      break;
    }
    case 10: {
      break;
    }
    case 11: {
      task_manager_list_hashable_f64_free(&ptr->val.float8_array);
      break;
    }
    case 12: {
      break;
    }
    case 13: {
      break;
    }
    case 14: {
      task_manager_list_hashable_f32_free(&ptr->val.float4_array);
      break;
    }
    case 15: {
      break;
    }
    case 16: {
      break;
    }
    case 17: {
      break;
    }
    case 18: {
      task_manager_list_s32_free(&ptr->val.int4_array);
      break;
    }
    case 19: {
      wasmcloud_postgres_types_numeric_free(&ptr->val.numeric);
      break;
    }
    case 20: {
      wasmcloud_postgres_types_numeric_free(&ptr->val.decimal);
      break;
    }
    case 21: {
      task_manager_list_numeric_free(&ptr->val.numeric_array);
      break;
    }
    case 22: {
      break;
    }
    case 23: {
      break;
    }
    case 24: {
      break;
    }
    case 25: {
      break;
    }
    case 26: {
      task_manager_list_s16_free(&ptr->val.int2_array);
      break;
    }
    case 27: {
      task_manager_list_s16_free(&ptr->val.int2_vector);
      break;
    }
    case 28: {
      task_manager_list_list_s16_free(&ptr->val.int2_vector_array);
      break;
    }
    case 29: {
      break;
    }
    case 30: {
      break;
    }
    case 31: {
      task_manager_tuple2_u32_list_u8_free(&ptr->val.bit);
      break;
    }
    case 32: {
      task_manager_list_tuple2_u32_list_u8_free(&ptr->val.bit_array);
      break;
    }
    case 33: {
      task_manager_tuple2_option_u32_list_u8_free(&ptr->val.bit_varying);
      break;
    }
    case 34: {
      task_manager_tuple2_option_u32_list_u8_free(&ptr->val.varbit);
      break;
    }
    case 35: {
      task_manager_list_tuple2_option_u32_list_u8_free(&ptr->val.varbit_array);
      break;
    }
    case 36: {
      task_manager_list_u8_free(&ptr->val.bytea);
      break;
    }
    case 37: {
      task_manager_list_list_u8_free(&ptr->val.bytea_array);
      break;
    }
    case 38: {
      task_manager_tuple2_u32_list_u8_free(&ptr->val.char_);
      break;
    }
    case 39: {
      task_manager_list_tuple2_u32_list_u8_free(&ptr->val.char_array);
      break;
    }
    case 40: {
      task_manager_tuple2_option_u32_list_u8_free(&ptr->val.varchar);
      break;
    }
    case 41: {
      task_manager_list_tuple2_option_u32_list_u8_free(&ptr->val.varchar_array);
      break;
    }
    case 42: {
      task_manager_string_free(&ptr->val.cidr);
      break;
    }
    case 43: {
      task_manager_list_string_free(&ptr->val.cidr_array);
      break;
    }
    case 44: {
      task_manager_string_free(&ptr->val.inet);
      break;
    }
    case 45: {
      task_manager_list_string_free(&ptr->val.inet_array);
      break;
    }
    case 46: {
      break;
    }
    case 47: {
      wasmcloud_postgres_types_list_mac_address_eui48_free(&ptr->val.macaddr_array);
      break;
    }
    case 48: {
      break;
    }
    case 49: {
      wasmcloud_postgres_types_list_mac_address_eui64_free(&ptr->val.macaddr8_array);
      break;
    }
    case 50: {
      break;
    }
    case 51: {
      task_manager_list_tuple2_lower_left_point_upper_right_point_free(&ptr->val.box_array);
      break;
    }
    case 52: {
      break;
    }
    case 53: {
      task_manager_list_tuple2_center_point_radius_free(&ptr->val.circle_array);
      break;
    }
    case 54: {
      break;
    }
    case 55: {
      task_manager_list_tuple2_start_point_end_point_free(&ptr->val.line_array);
      break;
    }
    case 56: {
      break;
    }
    case 57: {
      task_manager_list_tuple2_start_point_end_point_free(&ptr->val.lseg_array);
      break;
    }
    case 58: {
      task_manager_list_point_free(&ptr->val.path);
      break;
    }
    case 59: {
      task_manager_list_list_point_free(&ptr->val.path_array);
      break;
    }
    case 60: {
      break;
    }
    case 61: {
      task_manager_list_point_free(&ptr->val.point_array);
      break;
    }
    case 62: {
      task_manager_list_point_free(&ptr->val.polygon);
      break;
    }
    case 63: {
      task_manager_list_list_point_free(&ptr->val.polygon_array);
      break;
    }
    case 64: {
      wasmcloud_postgres_types_date_free(&ptr->val.date);
      break;
    }
    case 65: {
      wasmcloud_postgres_types_list_date_free(&ptr->val.date_array);
      break;
    }
    case 66: {
      wasmcloud_postgres_types_interval_free(&ptr->val.interval);
      break;
    }
    case 67: {
      wasmcloud_postgres_types_list_interval_free(&ptr->val.interval_array);
      break;
    }
    case 68: {
      break;
    }
    case 69: {
      wasmcloud_postgres_types_list_time_free(&ptr->val.time_array);
      break;
    }
    case 70: {
      wasmcloud_postgres_types_time_tz_free(&ptr->val.time_tz);
      break;
    }
    case 71: {
      wasmcloud_postgres_types_list_time_tz_free(&ptr->val.time_tz_array);
      break;
    }
    case 72: {
      wasmcloud_postgres_types_timestamp_free(&ptr->val.timestamp);
      break;
    }
    case 73: {
      wasmcloud_postgres_types_list_timestamp_free(&ptr->val.timestamp_array);
      break;
    }
    case 74: {
      wasmcloud_postgres_types_timestamp_tz_free(&ptr->val.timestamp_tz);
      break;
    }
    case 75: {
      wasmcloud_postgres_types_list_timestamp_tz_free(&ptr->val.timestamp_tz_array);
      break;
    }
    case 76: {
      task_manager_string_free(&ptr->val.json);
      break;
    }
    case 77: {
      task_manager_list_string_free(&ptr->val.json_array);
      break;
    }
    case 78: {
      task_manager_string_free(&ptr->val.jsonb);
      break;
    }
    case 79: {
      task_manager_list_string_free(&ptr->val.jsonb_array);
      break;
    }
    case 80: {
      wasmcloud_postgres_types_numeric_free(&ptr->val.money);
      break;
    }
    case 81: {
      task_manager_list_numeric_free(&ptr->val.money_array);
      break;
    }
    case 82: {
      break;
    }
    case 83: {
      task_manager_list_u64_free(&ptr->val.pg_lsn_array);
      break;
    }
    case 84: {
      task_manager_tuple3_xmin_xmax_xip_list_free(&ptr->val.pg_snapshot);
      break;
    }
    case 85: {
      break;
    }
    case 86: {
      task_manager_string_free(&ptr->val.name);
      break;
    }
    case 87: {
      task_manager_list_string_free(&ptr->val.name_array);
      break;
    }
    case 88: {
      task_manager_string_free(&ptr->val.text);
      break;
    }
    case 89: {
      task_manager_list_string_free(&ptr->val.text_array);
      break;
    }
    case 90: {
      task_manager_string_free(&ptr->val.xml);
      break;
    }
    case 91: {
      task_manager_list_string_free(&ptr->val.xml_array);
      break;
    }
    case 92: {
      task_manager_string_free(&ptr->val.ts_query);
      break;
    }
    case 93: {
      wasmcloud_postgres_types_list_lexeme_free(&ptr->val.ts_vector);
      break;
    }
    case 94: {
      task_manager_string_free(&ptr->val.uuid);
      break;
    }
    case 95: {
      task_manager_list_string_free(&ptr->val.uuid_array);
      break;
    }
    case 96: {
      task_manager_list_tuple2_string_option_string_free(&ptr->val.hstore);
      break;
    }
  }
}

void wasmcloud_postgres_types_result_row_entry_free(wasmcloud_postgres_types_result_row_entry_t *ptr) {
  task_manager_string_free(&ptr->column_name);
  wasmcloud_postgres_types_pg_value_free(&ptr->value);
}

void wasmcloud_postgres_types_result_row_free(wasmcloud_postgres_types_result_row_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_result_row_entry_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      wasmcloud_postgres_types_result_row_entry_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_query_pg_value_free(wasmcloud_postgres_query_pg_value_t *ptr) {
  wasmcloud_postgres_types_pg_value_free(ptr);
}

void wasmcloud_postgres_query_result_row_free(wasmcloud_postgres_query_result_row_t *ptr) {
  wasmcloud_postgres_types_result_row_free(ptr);
}

void wasmcloud_postgres_query_query_error_free(wasmcloud_postgres_query_query_error_t *ptr) {
  wasmcloud_postgres_types_query_error_free(ptr);
}

void wasmcloud_postgres_query_list_pg_value_free(wasmcloud_postgres_query_list_pg_value_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_query_pg_value_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      wasmcloud_postgres_query_pg_value_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_query_list_result_row_free(wasmcloud_postgres_query_list_result_row_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_query_result_row_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      wasmcloud_postgres_query_result_row_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_query_result_list_result_row_query_error_free(wasmcloud_postgres_query_result_list_result_row_query_error_t *ptr) {
  if (!ptr->is_err) {
    wasmcloud_postgres_query_list_result_row_free(&ptr->val.ok);
  } else {
    wasmcloud_postgres_query_query_error_free(&ptr->val.err);
  }
}

void wasmcloud_postgres_prepared_pg_value_free(wasmcloud_postgres_prepared_pg_value_t *ptr) {
  wasmcloud_postgres_types_pg_value_free(ptr);
}

void wasmcloud_postgres_prepared_result_row_free(wasmcloud_postgres_prepared_result_row_t *ptr) {
  wasmcloud_postgres_types_result_row_free(ptr);
}

void wasmcloud_postgres_prepared_statement_prepare_error_free(wasmcloud_postgres_prepared_statement_prepare_error_t *ptr) {
  wasmcloud_postgres_types_statement_prepare_error_free(ptr);
}

void wasmcloud_postgres_prepared_prepared_statement_exec_error_free(wasmcloud_postgres_prepared_prepared_statement_exec_error_t *ptr) {
  wasmcloud_postgres_types_prepared_statement_exec_error_free(ptr);
}

void wasmcloud_postgres_prepared_prepared_statement_token_free(wasmcloud_postgres_prepared_prepared_statement_token_t *ptr) {
  task_manager_string_free(ptr);
}

void wasmcloud_postgres_prepared_result_prepared_statement_token_statement_prepare_error_free(wasmcloud_postgres_prepared_result_prepared_statement_token_statement_prepare_error_t *ptr) {
  if (!ptr->is_err) {
    wasmcloud_postgres_prepared_prepared_statement_token_free(&ptr->val.ok);
  } else {
    wasmcloud_postgres_prepared_statement_prepare_error_free(&ptr->val.err);
  }
}

void wasmcloud_postgres_prepared_list_pg_value_free(wasmcloud_postgres_prepared_list_pg_value_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_prepared_pg_value_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      wasmcloud_postgres_prepared_pg_value_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_prepared_result_u64_prepared_statement_exec_error_free(wasmcloud_postgres_prepared_result_u64_prepared_statement_exec_error_t *ptr) {
  if (!ptr->is_err) {
  } else {
    wasmcloud_postgres_prepared_prepared_statement_exec_error_free(&ptr->val.err);
  }
}

void wasmcloud_task_manager_types_operation_error_free(wasmcloud_task_manager_types_operation_error_t *ptr) {
  task_manager_string_free(&ptr->message);
}

void wasmcloud_task_manager_types_operation_free(wasmcloud_task_manager_types_operation_t *ptr) {
  task_manager_string_free(&ptr->id);
  task_manager_string_free(&ptr->category);
  task_manager_string_free(&ptr->payload);
  task_manager_string_free(&ptr->created_at);
}

void exports_wasmcloud_task_manager_tracker_operation_free(exports_wasmcloud_task_manager_tracker_operation_t *ptr) {
  wasmcloud_task_manager_types_operation_free(ptr);
}

void exports_wasmcloud_task_manager_tracker_operation_error_free(exports_wasmcloud_task_manager_tracker_operation_error_t *ptr) {
  wasmcloud_task_manager_types_operation_error_free(ptr);
}

void exports_wasmcloud_task_manager_tracker_result_string_operation_error_free(exports_wasmcloud_task_manager_tracker_result_string_operation_error_t *ptr) {
  if (!ptr->is_err) {
    task_manager_string_free(&ptr->val.ok);
  } else {
    exports_wasmcloud_task_manager_tracker_operation_error_free(&ptr->val.err);
  }
}

void exports_wasmcloud_task_manager_tracker_result_operation_operation_error_free(exports_wasmcloud_task_manager_tracker_result_operation_operation_error_t *ptr) {
  if (!ptr->is_err) {
    exports_wasmcloud_task_manager_tracker_operation_free(&ptr->val.ok);
  } else {
    exports_wasmcloud_task_manager_tracker_operation_error_free(&ptr->val.err);
  }
}

void exports_wasmcloud_task_manager_tracker_result_void_operation_error_free(exports_wasmcloud_task_manager_tracker_result_void_operation_error_t *ptr) {
  if (!ptr->is_err) {
  } else {
    exports_wasmcloud_task_manager_tracker_operation_error_free(&ptr->val.err);
  }
}

void exports_wasmcloud_task_manager_tracker_list_operation_free(exports_wasmcloud_task_manager_tracker_list_operation_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    exports_wasmcloud_task_manager_tracker_operation_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      exports_wasmcloud_task_manager_tracker_operation_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void exports_wasmcloud_task_manager_tracker_result_list_operation_operation_error_free(exports_wasmcloud_task_manager_tracker_result_list_operation_operation_error_t *ptr) {
  if (!ptr->is_err) {
    exports_wasmcloud_task_manager_tracker_list_operation_free(&ptr->val.ok);
  } else {
    exports_wasmcloud_task_manager_tracker_operation_error_free(&ptr->val.err);
  }
}

void task_manager_string_set(task_manager_string_t *ret, const char*s) {
  ret->ptr = (uint8_t*) s;
  ret->len = strlen(s);
}

void task_manager_string_dup(task_manager_string_t *ret, const char*s) {
  ret->len = strlen(s);
  ret->ptr = (uint8_t*) cabi_realloc(NULL, 0, 1, ret->len * 1);
  memcpy(ret->ptr, s, ret->len * 1);
}

void task_manager_string_free(task_manager_string_t *ret) {
  if (ret->len > 0) {
    free(ret->ptr);
  }
  ret->ptr = NULL;
  ret->len = 0;
}

// Component Adapters

__attribute__((__aligned__(4)))
static uint8_t RET_AREA[72];

void wasi_logging_logging_log(wasi_logging_logging_level_t level, task_manager_string_t *context, task_manager_string_t *message) {
  __wasm_import_wasi_logging_logging_log((int32_t) level, (uint8_t *) (*context).ptr, (*context).len, (uint8_t *) (*message).ptr, (*message).len);
}

void wasmcloud_postgres_query_query(task_manager_string_t *query, wasmcloud_postgres_query_list_pg_value_t *params, wasmcloud_postgres_query_result_list_result_row_query_error_t *ret) {
  __attribute__((__aligned__(4)))
  uint8_t ret_area[16];
  uint8_t *ptr = (uint8_t *) &ret_area;
  __wasm_import_wasmcloud_postgres_query_query((uint8_t *) (*query).ptr, (*query).len, (uint8_t *) (*params).ptr, (*params).len, ptr);
  wasmcloud_postgres_query_result_list_result_row_query_error_t result;
  switch ((int32_t) *((uint8_t*) (ptr + 0))) {
    case 0: {
      result.is_err = false;
      result.val.ok = (wasmcloud_postgres_query_list_result_row_t) { (wasmcloud_postgres_query_result_row_t*)(*((uint8_t **) (ptr + 4))), (*((size_t*) (ptr + 8))) };
      break;
    }
    case 1: {
      result.is_err = true;
      wasmcloud_postgres_types_query_error_t variant165;
      variant165.tag = (int32_t) *((uint8_t*) (ptr + 4));
      switch ((int32_t) variant165.tag) {
        case 0: {
          variant165.val.invalid_params = (task_manager_string_t) { (uint8_t*)(*((uint8_t **) (ptr + 8))), (*((size_t*) (ptr + 12))) };
          break;
        }
        case 1: {
          variant165.val.invalid_query = (task_manager_string_t) { (uint8_t*)(*((uint8_t **) (ptr + 8))), (*((size_t*) (ptr + 12))) };
          break;
        }
        case 2: {
          variant165.val.unexpected = (task_manager_string_t) { (uint8_t*)(*((uint8_t **) (ptr + 8))), (*((size_t*) (ptr + 12))) };
          break;
        }
      }

      result.val.err = variant165;
      break;
    }
  }
  *ret = result;
}

void wasmcloud_postgres_prepared_prepare(task_manager_string_t *statement, wasmcloud_postgres_prepared_result_prepared_statement_token_statement_prepare_error_t *ret) {
  __attribute__((__aligned__(4)))
  uint8_t ret_area[16];
  uint8_t *ptr = (uint8_t *) &ret_area;
  __wasm_import_wasmcloud_postgres_prepared_prepare((uint8_t *) (*statement).ptr, (*statement).len, ptr);
  wasmcloud_postgres_prepared_result_prepared_statement_token_statement_prepare_error_t result;
  switch ((int32_t) *((uint8_t*) (ptr + 0))) {
    case 0: {
      result.is_err = false;
      result.val.ok = (task_manager_string_t) { (uint8_t*)(*((uint8_t **) (ptr + 4))), (*((size_t*) (ptr + 8))) };
      break;
    }
    case 1: {
      result.is_err = true;
      wasmcloud_postgres_types_statement_prepare_error_t variant;
      variant.tag = (int32_t) *((uint8_t*) (ptr + 4));
      switch ((int32_t) variant.tag) {
        case 0: {
          variant.val.unexpected = (task_manager_string_t) { (uint8_t*)(*((uint8_t **) (ptr + 8))), (*((size_t*) (ptr + 12))) };
          break;
        }
      }

      result.val.err = variant;
      break;
    }
  }
  *ret = result;
}

void wasmcloud_postgres_prepared_exec(wasmcloud_postgres_prepared_prepared_statement_token_t *stmt_token, wasmcloud_postgres_prepared_list_pg_value_t *params, wasmcloud_postgres_prepared_result_u64_prepared_statement_exec_error_t *ret) {
  __attribute__((__aligned__(8)))
  uint8_t ret_area[24];
  uint8_t *ptr = (uint8_t *) &ret_area;
  __wasm_import_wasmcloud_postgres_prepared_exec((uint8_t *) (*stmt_token).ptr, (*stmt_token).len, (uint8_t *) (*params).ptr, (*params).len, ptr);
  wasmcloud_postgres_prepared_result_u64_prepared_statement_exec_error_t result;
  switch ((int32_t) *((uint8_t*) (ptr + 0))) {
    case 0: {
      result.is_err = false;
      result.val.ok = (uint64_t) (*((int64_t*) (ptr + 8)));
      break;
    }
    case 1: {
      result.is_err = true;
      wasmcloud_postgres_types_prepared_statement_exec_error_t variant146;
      variant146.tag = (int32_t) *((uint8_t*) (ptr + 8));
      switch ((int32_t) variant146.tag) {
        case 0: {
          break;
        }
        case 1: {
          wasmcloud_postgres_types_query_error_t variant;
          variant.tag = (int32_t) *((uint8_t*) (ptr + 12));
          switch ((int32_t) variant.tag) {
            case 0: {
              variant.val.invalid_params = (task_manager_string_t) { (uint8_t*)(*((uint8_t **) (ptr + 16))), (*((size_t*) (ptr + 20))) };
              break;
            }
            case 1: {
              variant.val.invalid_query = (task_manager_string_t) { (uint8_t*)(*((uint8_t **) (ptr + 16))), (*((size_t*) (ptr + 20))) };
              break;
            }
            case 2: {
              variant.val.unexpected = (task_manager_string_t) { (uint8_t*)(*((uint8_t **) (ptr + 16))), (*((size_t*) (ptr + 20))) };
              break;
            }
          }
          variant146.val.query_error = variant;
          break;
        }
        case 2: {
          variant146.val.unexpected = (task_manager_string_t) { (uint8_t*)(*((uint8_t **) (ptr + 12))), (*((size_t*) (ptr + 16))) };
          break;
        }
      }

      result.val.err = variant146;
      break;
    }
  }
  *ret = result;
}

__attribute__((__export_name__("wasmcloud:task-manager/tracker#start")))
uint8_t * __wasm_export_exports_wasmcloud_task_manager_tracker_start(uint8_t * arg, size_t arg0, uint8_t * arg1, size_t arg2) {
  task_manager_string_t arg3 = (task_manager_string_t) { (uint8_t*)(arg), (arg0) };
  task_manager_string_t arg4 = (task_manager_string_t) { (uint8_t*)(arg1), (arg2) };
  exports_wasmcloud_task_manager_tracker_result_string_operation_error_t ret;
  exports_wasmcloud_task_manager_tracker_start(&arg3, &arg4, &ret);
  uint8_t *ptr = (uint8_t *) &RET_AREA;
  if ((ret).is_err) {
    const exports_wasmcloud_task_manager_tracker_operation_error_t *payload5 = &(ret).val.err;*((int8_t*)(ptr + 0)) = 1;
    *((size_t*)(ptr + 8)) = ((*payload5).message).len;
    *((uint8_t **)(ptr + 4)) = (uint8_t *) ((*payload5).message).ptr;
  } else {
    const task_manager_string_t *payload = &(ret).val.ok;*((int8_t*)(ptr + 0)) = 0;
    *((size_t*)(ptr + 8)) = (*payload).len;
    *((uint8_t **)(ptr + 4)) = (uint8_t *) (*payload).ptr;
  }
  return ptr;
}

__attribute__((__export_name__("wasmcloud:task-manager/tracker#get")))
uint8_t * __wasm_export_exports_wasmcloud_task_manager_tracker_get(uint8_t * arg, size_t arg0) {
  task_manager_string_t arg1 = (task_manager_string_t) { (uint8_t*)(arg), (arg0) };
  exports_wasmcloud_task_manager_tracker_result_operation_operation_error_t ret;
  exports_wasmcloud_task_manager_tracker_get(&arg1, &ret);
  uint8_t *ptr = (uint8_t *) &RET_AREA;
  if ((ret).is_err) {
    const exports_wasmcloud_task_manager_tracker_operation_error_t *payload8 = &(ret).val.err;*((int8_t*)(ptr + 0)) = 1;
    *((size_t*)(ptr + 8)) = ((*payload8).message).len;
    *((uint8_t **)(ptr + 4)) = (uint8_t *) ((*payload8).message).ptr;
  } else {
    const exports_wasmcloud_task_manager_tracker_operation_t *payload = &(ret).val.ok;*((int8_t*)(ptr + 0)) = 0;
    *((size_t*)(ptr + 8)) = ((*payload).id).len;
    *((uint8_t **)(ptr + 4)) = (uint8_t *) ((*payload).id).ptr;
    *((size_t*)(ptr + 16)) = ((*payload).category).len;
    *((uint8_t **)(ptr + 12)) = (uint8_t *) ((*payload).category).ptr;
    *((size_t*)(ptr + 24)) = ((*payload).payload).len;
    *((uint8_t **)(ptr + 20)) = (uint8_t *) ((*payload).payload).ptr;
    *((size_t*)(ptr + 32)) = ((*payload).created_at).len;
    *((uint8_t **)(ptr + 28)) = (uint8_t *) ((*payload).created_at).ptr;
    if (((*payload).completed_at).is_some) {
      const task_manager_string_t *payload3 = &((*payload).completed_at).val;
      *((int8_t*)(ptr + 36)) = 1;
      *((size_t*)(ptr + 44)) = (*payload3).len;
      *((uint8_t **)(ptr + 40)) = (uint8_t *) (*payload3).ptr;
    } else {
      *((int8_t*)(ptr + 36)) = 0;
    }
    if (((*payload).failure).is_some) {
      const task_manager_string_t *payload5 = &((*payload).failure).val;
      *((int8_t*)(ptr + 48)) = 1;
      *((size_t*)(ptr + 56)) = (*payload5).len;
      *((uint8_t **)(ptr + 52)) = (uint8_t *) (*payload5).ptr;
    } else {
      *((int8_t*)(ptr + 48)) = 0;
    }
    if (((*payload).result).is_some) {
      const task_manager_string_t *payload7 = &((*payload).result).val;
      *((int8_t*)(ptr + 60)) = 1;
      *((size_t*)(ptr + 68)) = (*payload7).len;
      *((uint8_t **)(ptr + 64)) = (uint8_t *) (*payload7).ptr;
    } else {
      *((int8_t*)(ptr + 60)) = 0;
    }
  }
  return ptr;
}

__attribute__((__export_name__("wasmcloud:task-manager/tracker#complete")))
uint8_t * __wasm_export_exports_wasmcloud_task_manager_tracker_complete(uint8_t * arg, size_t arg0, uint8_t * arg1, size_t arg2) {
  task_manager_string_t arg3 = (task_manager_string_t) { (uint8_t*)(arg), (arg0) };
  task_manager_string_t arg4 = (task_manager_string_t) { (uint8_t*)(arg1), (arg2) };
  exports_wasmcloud_task_manager_tracker_result_void_operation_error_t ret;
  exports_wasmcloud_task_manager_tracker_complete(&arg3, &arg4, &ret);
  uint8_t *ptr = (uint8_t *) &RET_AREA;
  if ((ret).is_err) {
    const exports_wasmcloud_task_manager_tracker_operation_error_t *payload5 = &(ret).val.err;*((int8_t*)(ptr + 0)) = 1;
    *((size_t*)(ptr + 8)) = ((*payload5).message).len;
    *((uint8_t **)(ptr + 4)) = (uint8_t *) ((*payload5).message).ptr;
  } else {
    *((int8_t*)(ptr + 0)) = 0;
  }
  return ptr;
}

__attribute__((__export_name__("wasmcloud:task-manager/tracker#fail")))
uint8_t * __wasm_export_exports_wasmcloud_task_manager_tracker_fail(uint8_t * arg, size_t arg0, uint8_t * arg1, size_t arg2) {
  task_manager_string_t arg3 = (task_manager_string_t) { (uint8_t*)(arg), (arg0) };
  task_manager_string_t arg4 = (task_manager_string_t) { (uint8_t*)(arg1), (arg2) };
  exports_wasmcloud_task_manager_tracker_result_void_operation_error_t ret;
  exports_wasmcloud_task_manager_tracker_fail(&arg3, &arg4, &ret);
  uint8_t *ptr = (uint8_t *) &RET_AREA;
  if ((ret).is_err) {
    const exports_wasmcloud_task_manager_tracker_operation_error_t *payload5 = &(ret).val.err;*((int8_t*)(ptr + 0)) = 1;
    *((size_t*)(ptr + 8)) = ((*payload5).message).len;
    *((uint8_t **)(ptr + 4)) = (uint8_t *) ((*payload5).message).ptr;
  } else {
    *((int8_t*)(ptr + 0)) = 0;
  }
  return ptr;
}

__attribute__((__export_name__("wasmcloud:task-manager/tracker#list")))
uint8_t * __wasm_export_exports_wasmcloud_task_manager_tracker_list(int32_t arg, uint8_t * arg0, size_t arg1) {
  task_manager_option_string_t option;
  switch (arg) {
    case 0: {
      option.is_some = false;
      break;
    }
    case 1: {
      option.is_some = true;
      option.val = (task_manager_string_t) { (uint8_t*)(arg0), (arg1) };
      break;
    }
  }
  task_manager_option_string_t arg2 = option;
  exports_wasmcloud_task_manager_tracker_result_list_operation_operation_error_t ret;
  exports_wasmcloud_task_manager_tracker_list(&arg2, &ret);
  uint8_t *ptr = (uint8_t *) &RET_AREA;
  if ((ret).is_err) {
    const exports_wasmcloud_task_manager_tracker_operation_error_t *payload9 = &(ret).val.err;*((int8_t*)(ptr + 0)) = 1;
    *((size_t*)(ptr + 8)) = ((*payload9).message).len;
    *((uint8_t **)(ptr + 4)) = (uint8_t *) ((*payload9).message).ptr;
  } else {
    const exports_wasmcloud_task_manager_tracker_list_operation_t *payload = &(ret).val.ok;*((int8_t*)(ptr + 0)) = 0;
    *((size_t*)(ptr + 8)) = (*payload).len;
    *((uint8_t **)(ptr + 4)) = (uint8_t *) (*payload).ptr;
  }
  return ptr;
}

__attribute__((__export_name__("wasmcloud:task-manager/tracker#delete")))
uint8_t * __wasm_export_exports_wasmcloud_task_manager_tracker_delete(uint8_t * arg, size_t arg0) {
  task_manager_string_t arg1 = (task_manager_string_t) { (uint8_t*)(arg), (arg0) };
  exports_wasmcloud_task_manager_tracker_result_void_operation_error_t ret;
  exports_wasmcloud_task_manager_tracker_delete(&arg1, &ret);
  uint8_t *ptr = (uint8_t *) &RET_AREA;
  if ((ret).is_err) {
    const exports_wasmcloud_task_manager_tracker_operation_error_t *payload2 = &(ret).val.err;*((int8_t*)(ptr + 0)) = 1;
    *((size_t*)(ptr + 8)) = ((*payload2).message).len;
    *((uint8_t **)(ptr + 4)) = (uint8_t *) ((*payload2).message).ptr;
  } else {
    *((int8_t*)(ptr + 0)) = 0;
  }
  return ptr;
}

// Ensure that the *_component_type.o object is linked in

extern void __component_type_object_force_link_task_manager(void);
void __component_type_object_force_link_task_manager_public_use_in_this_compilation_unit(void) {
  __component_type_object_force_link_task_manager();
}
